diff --git a/docs/reviews/runtime-completion-2026-09-17.md b/docs/reviews/runtime-completion-2026-09-17.md
index d13831933077dccfa924942c21479526108cbdba..a5b51d2dbc231015185b8dc9c27844cfbc854f81 100644
--- a/docs/reviews/runtime-completion-2026-09-17.md
+++ b/docs/reviews/runtime-completion-2026-09-17.md
@@ -37 +37 @@ The existing deterministic CPU trainer remains a development baseline.
-   timeouts are integer milliseconds in [0,1000]. Initialization, response grace
+   timeouts are integer milliseconds in [0,2000]. Initialization, response grace
@@ -131,0 +132,9 @@ make historical release evidence valid for a different tree.
+
+## Windows compatibility correction
+
+The first full Windows validation failed the existing WGC and DXGI tests because
+our new 1000ms admission cap rejected their established 2000ms timeout. The
+follow-up preserves the existing two-second capture API while still rejecting
+negative, boolean, overflowing and greater-than-2000ms inputs in Python and Rust.
+Both original Windows tests remain enabled and unchanged. This is an API
+compatibility fix, not an increased inference/recording budget or a skipped test.
diff --git a/native/crates/uga-capture/src/ffi.rs b/native/crates/uga-capture/src/ffi.rs
index b81abbfb749e47e47472a5f3931ef36a86c03b1c..4bee71ca6f8632e69f6df712479eeb0448395555 100644
--- a/native/crates/uga-capture/src/ffi.rs
+++ b/native/crates/uga-capture/src/ffi.rs
@@ -28 +28 @@ const CLOSE_DEADLINE_MS: u64 = 2_000;
-const MAX_CAPTURE_TIMEOUT_MS: u32 = 1_000;
+const MAX_CAPTURE_TIMEOUT_MS: u32 = 2_000;
diff --git a/tests/unit/test_native_lifecycle_bounds.py b/tests/unit/test_native_lifecycle_bounds.py
index 23baf689e1330d243234b113b710ee7743d5e874..c8bc504ed6837cab9c71f56168a679bffeb1bc97 100644
--- a/tests/unit/test_native_lifecycle_bounds.py
+++ b/tests/unit/test_native_lifecycle_bounds.py
@@ -77 +77 @@ class NativeLifecycleBoundsTests(unittest.TestCase):
-        for value in (-1, True, 1001, 1 << 33):
+        for value in (-1, True, 2001, 1 << 33):
@@ -82,0 +83,12 @@ class NativeLifecycleBoundsTests(unittest.TestCase):
+
+    def test_existing_two_second_capture_configuration_remains_supported(self) -> None:
+        windows = FakeWindows()
+        driver = CtypesNativeCaptureDriver(
+            NativeBackendId.WGC, cast(NativeCaptureLibrary, FakeLibrary(windows)),
+            cast(WindowBackend, windows), timeout_ms=2000,
+        )
+        driver.start(windows.identity)
+        try:
+            self.assertIsNotNone(driver.capture())
+        finally:
+            driver.stop()
diff --git a/uga/capture/native_ctypes.py b/uga/capture/native_ctypes.py
index 15769eff2a219f83340fb8d742d6c6b618c888ff..10af49b5e2cc5871afd46b8e356bd3abab95fb13 100644
--- a/uga/capture/native_ctypes.py
+++ b/uga/capture/native_ctypes.py
@@ -35,0 +36,2 @@ _SAFE_DLL_SEARCH = 0x00000100 | 0x00000800
+# Preserve the existing WGC/DXGI 2s startup capture contract.
+_MAX_CAPTURE_TIMEOUT_MS = 2_000
@@ -157,2 +159,2 @@ class NativeCaptureLibrary:
-        if type(timeout_ms) is not int or not 0 <= timeout_ms <= 1000:
-            raise BackendUnavailableError("capture timeout must be an integer in [0, 1000] ms")
+        if type(timeout_ms) is not int or not 0 <= timeout_ms <= _MAX_CAPTURE_TIMEOUT_MS:
+            raise BackendUnavailableError("capture timeout must be an integer in [0, 2000] ms")
@@ -223,2 +225,2 @@ class CtypesNativeCaptureDriver(NativeCaptureDriver):
-        if type(timeout_ms) is not int or not 0 <= timeout_ms <= 1000:
-            raise BackendUnavailableError("capture timeout must be an integer in [0, 1000] ms")
+        if type(timeout_ms) is not int or not 0 <= timeout_ms <= _MAX_CAPTURE_TIMEOUT_MS:
+            raise BackendUnavailableError("capture timeout must be an integer in [0, 2000] ms")
