stage: motor_bc
base_model: UGA-Fixture-Pixel-Profile-Encoder
freeze_visual_layers: true
train_components: [axis_linear_head, button_bit_prior, residual_confidence]
tick_rate_hz: 30
action_horizon: 6
epochs: 200
learning_rate: 0.05
loss:
  movement: mse
  camera: huber
  buttons: bitwise_prior_accuracy
  confidence: residual_calibration
